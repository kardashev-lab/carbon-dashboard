# Local dev: proxy through Next.js to avoid CORS (API allows only *.kardashevlabs.org)
# Production: omit this env var; the client hits data.kardashevlabs.org directly
NEXT_PUBLIC_API_URL=http://localhost:3000/api/proxy
